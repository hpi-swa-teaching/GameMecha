accessing
stepTime
	"This should not be a final fix for glitching behavior. The submorphs of a CollisionWorld-PasteUpMorph should only update their state when their CollisionWorld-PasteUpMorph tells them to do so, not when the World tells them to do so"
	^10000000.