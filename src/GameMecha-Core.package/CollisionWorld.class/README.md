A CollisionWorld is a Parent-Morph that handles collisions inside its children.

Instance Variables
	collisionHandler:		BlockClosure

collisionHandler
	- the one Block that gets executed whenever things collide
