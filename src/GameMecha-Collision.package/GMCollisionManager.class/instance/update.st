collision
update
	"
	Update should be called on every tick of your game. 
	Once the collision manager has been prompted to update, it will notify all colliding morphs via the `onCollision: aCollisionEvent` API.
	"

	self checkCollisions