A GMMorphCollisionEvent is a collision event that happened between morphs. It is the top level collision event that is passed in when onCollision: is called by the GMCollisionManager.

Instance Variables
	other:		<Morph>
	own:		<Morph>

own
	- the morph itself
	
other
	- the morph it collided with


