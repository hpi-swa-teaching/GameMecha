A CollisionWorld is A Parent-Morph that handles collisions inside his children.

Instance Variables
	collisionHandler:		BlockClosure

collisionHandler
	- the one Block that gets executed whenever things collide
